# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.flow_providers_response import FlowProvidersResponse
from ..types.voices_dto import VoicesDto
from .raw_client import AsyncRawAiModelsClient, RawAiModelsClient


class AiModelsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAiModelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAiModelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAiModelsClient
        """
        return self._raw_client

    def list_providers_public(
        self, *, use_case: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> FlowProvidersResponse:
        """
        GET /api/v1/models

        Parameters
        ----------
        use_case : typing.Optional[str]
            Filter by use case

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlowProvidersResponse
            All providers with models and pricing (public)

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.ai_models.list_providers_public()
        """
        _response = self._raw_client.list_providers_public(use_case=use_case, request_options=request_options)
        return _response.data

    def list_tts_voices(
        self,
        *,
        page_size: typing.Optional[int] = None,
        next_page_token: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        voice_type: typing.Optional[str] = None,
        category: typing.Optional[str] = None,
        gender: typing.Optional[str] = None,
        age: typing.Optional[str] = None,
        language: typing.Optional[str] = None,
        accent: typing.Optional[str] = None,
        sort: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoicesDto:
        """
        GET /api/v1/models/tts/voices

        When any of gender, age, language, accent, or sort are present, routes to
        the ElevenLabs /v1/shared-voices endpoint (rich filtering, offset pagination).
        Otherwise routes to /v2/voices (simpler, cursor pagination).

        Parameters
        ----------
        page_size : typing.Optional[int]
            Page size (max 100, default 50)

        next_page_token : typing.Optional[str]
            Pagination token for next page

        search : typing.Optional[str]
            Search term

        voice_type : typing.Optional[str]
            Voice type filter (v2 API)

        category : typing.Optional[str]
            Category filter

        gender : typing.Optional[str]
            Gender filter (male, female, neutral) — shared-voices API

        age : typing.Optional[str]
            Age group filter (young, middle_aged, old) — shared-voices API

        language : typing.Optional[str]
            Language filter (ISO code) — shared-voices API

        accent : typing.Optional[str]
            Accent filter — shared-voices API

        sort : typing.Optional[str]
            Sort order (trending, latest, most_users) — shared-voices API

        page : typing.Optional[int]
            0-based page offset — shared-voices API

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoicesDto
            ElevenLabs TTS voices

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.ai_models.list_tts_voices()
        """
        _response = self._raw_client.list_tts_voices(
            page_size=page_size,
            next_page_token=next_page_token,
            search=search,
            voice_type=voice_type,
            category=category,
            gender=gender,
            age=age,
            language=language,
            accent=accent,
            sort=sort,
            page=page,
            request_options=request_options,
        )
        return _response.data


class AsyncAiModelsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAiModelsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAiModelsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAiModelsClient
        """
        return self._raw_client

    async def list_providers_public(
        self, *, use_case: typing.Optional[str] = None, request_options: typing.Optional[RequestOptions] = None
    ) -> FlowProvidersResponse:
        """
        GET /api/v1/models

        Parameters
        ----------
        use_case : typing.Optional[str]
            Filter by use case

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        FlowProvidersResponse
            All providers with models and pricing (public)

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ai_models.list_providers_public()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_providers_public(use_case=use_case, request_options=request_options)
        return _response.data

    async def list_tts_voices(
        self,
        *,
        page_size: typing.Optional[int] = None,
        next_page_token: typing.Optional[str] = None,
        search: typing.Optional[str] = None,
        voice_type: typing.Optional[str] = None,
        category: typing.Optional[str] = None,
        gender: typing.Optional[str] = None,
        age: typing.Optional[str] = None,
        language: typing.Optional[str] = None,
        accent: typing.Optional[str] = None,
        sort: typing.Optional[str] = None,
        page: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> VoicesDto:
        """
        GET /api/v1/models/tts/voices

        When any of gender, age, language, accent, or sort are present, routes to
        the ElevenLabs /v1/shared-voices endpoint (rich filtering, offset pagination).
        Otherwise routes to /v2/voices (simpler, cursor pagination).

        Parameters
        ----------
        page_size : typing.Optional[int]
            Page size (max 100, default 50)

        next_page_token : typing.Optional[str]
            Pagination token for next page

        search : typing.Optional[str]
            Search term

        voice_type : typing.Optional[str]
            Voice type filter (v2 API)

        category : typing.Optional[str]
            Category filter

        gender : typing.Optional[str]
            Gender filter (male, female, neutral) — shared-voices API

        age : typing.Optional[str]
            Age group filter (young, middle_aged, old) — shared-voices API

        language : typing.Optional[str]
            Language filter (ISO code) — shared-voices API

        accent : typing.Optional[str]
            Accent filter — shared-voices API

        sort : typing.Optional[str]
            Sort order (trending, latest, most_users) — shared-voices API

        page : typing.Optional[int]
            0-based page offset — shared-voices API

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        VoicesDto
            ElevenLabs TTS voices

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.ai_models.list_tts_voices()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_tts_voices(
            page_size=page_size,
            next_page_token=next_page_token,
            search=search,
            voice_type=voice_type,
            category=category,
            gender=gender,
            age=age,
            language=language,
            accent=accent,
            sort=sort,
            page=page,
            request_options=request_options,
        )
        return _response.data
