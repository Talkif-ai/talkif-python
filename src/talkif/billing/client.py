# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.analytics_cost_breakdown_response import AnalyticsCostBreakdownResponse
from ..types.balance_summary_response import BalanceSummaryResponse
from ..types.call_cost_breakdown_response import CallCostBreakdownResponse
from ..types.charge_detail_response import ChargeDetailResponse
from ..types.charge_status import ChargeStatus
from ..types.charge_type_db import ChargeTypeDb
from ..types.invoice_response import InvoiceResponse
from ..types.paginated_response import PaginatedResponse
from ..types.public_pricing_response import PublicPricingResponse
from .raw_client import AsyncRawBillingClient, RawBillingClient


class BillingClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawBillingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawBillingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawBillingClient
        """
        return self._raw_client

    def get_balance_summary(self, *, request_options: typing.Optional[RequestOptions] = None) -> BalanceSummaryResponse:
        """
        GET /api/v1/billing/balances

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BalanceSummaryResponse
            Balance summary

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_balance_summary()
        """
        _response = self._raw_client.get_balance_summary(request_options=request_options)
        return _response.data

    def list_charges(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        charge_type: typing.Optional[ChargeTypeDb] = None,
        status: typing.Optional[ChargeStatus] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        Paginated, filterable charge history for an account.
        Returns charges with entity context (phone number, flow name, contact name)
        from the first associated cost line item.

        Parameters
        ----------
        limit : typing.Optional[int]
            Page size (max 100)

        offset : typing.Optional[int]
            Page offset

        charge_type : typing.Optional[ChargeTypeDb]
            Filter by charge type

        status : typing.Optional[ChargeStatus]
            Filter by status

        start_date : typing.Optional[str]
            Filter from date (inclusive)

        end_date : typing.Optional[str]
            Filter to date (inclusive)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Paginated charge history

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.list_charges()
        """
        _response = self._raw_client.list_charges(
            limit=limit,
            offset=offset,
            charge_type=charge_type,
            status=status,
            start_date=start_date,
            end_date=end_date,
            request_options=request_options,
        )
        return _response.data

    def get_charge_detail(
        self, charge_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ChargeDetailResponse:
        """
        Detailed charge view with cost line items breakdown.

        Parameters
        ----------
        charge_id : str
            Charge ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChargeDetailResponse
            Charge detail with line items

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_charge_detail(
            charge_id="chargeId",
        )
        """
        _response = self._raw_client.get_charge_detail(charge_id, request_options=request_options)
        return _response.data

    def get_billing_cost_breakdown(
        self,
        *,
        period: typing.Optional[str] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnalyticsCostBreakdownResponse:
        """
        Get account-level cost breakdown by category with drill-down to calls.

        ## Query Parameters
        - `period`: `today`, `week`, `month`, or `custom` (default: `week`)
        - `startDate`: Required when `period=custom`
        - `endDate`: Required when `period=custom`

        ## Response
        Returns total costs, breakdown by category (LLM, STT, TTS, telephony),
        breakdown by flow, and recent calls with individual cost breakdowns.

        Parameters
        ----------
        period : typing.Optional[str]
            Time period: today, week, month, or custom

        start_date : typing.Optional[str]
            Start date for custom period

        end_date : typing.Optional[str]
            End date for custom period

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnalyticsCostBreakdownResponse
            Cost breakdown by category

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_billing_cost_breakdown()
        """
        _response = self._raw_client.get_billing_cost_breakdown(
            period=period, start_date=start_date, end_date=end_date, request_options=request_options
        )
        return _response.data

    def get_billing_call_cost_breakdown(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CallCostBreakdownResponse:
        """
        Get detailed cost breakdown for a single call with line items.

        Returns all cost line items including usage data (tokens, seconds, characters)
        and rate information for each cost component.

        Parameters
        ----------
        call_id : str
            Call ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CallCostBreakdownResponse
            Call cost breakdown with line items

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_billing_call_cost_breakdown(
            call_id="callId",
        )
        """
        _response = self._raw_client.get_billing_call_cost_breakdown(call_id, request_options=request_options)
        return _response.data

    def list_invoices(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        GET /api/v1/billing/invoices

        Parameters
        ----------
        limit : typing.Optional[int]
            Page size

        offset : typing.Optional[int]
            Page offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Paginated invoice list

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.list_invoices()
        """
        _response = self._raw_client.list_invoices(limit=limit, offset=offset, request_options=request_options)
        return _response.data

    def get_invoice(
        self, invoice_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> InvoiceResponse:
        """
        GET /api/v1/billing/invoices/{invoiceId}

        Parameters
        ----------
        invoice_id : str
            Invoice ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InvoiceResponse
            Invoice details

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_invoice(
            invoice_id="invoiceId",
        )
        """
        _response = self._raw_client.get_invoice(invoice_id, request_options=request_options)
        return _response.data

    def get_transaction_history(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        GET /api/v1/billing/transactions

        Parameters
        ----------
        limit : typing.Optional[int]
            Max items to return

        offset : typing.Optional[int]
            Items to skip

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Transaction history

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_transaction_history()
        """
        _response = self._raw_client.get_transaction_history(
            limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def get_public_pricing(self, *, request_options: typing.Optional[RequestOptions] = None) -> PublicPricingResponse:
        """
        Public endpoint — no authentication required.
        Returns telephony rates, phone number pricing, and recording storage pricing.

        LLM/STT/TTS pricing is served by `GET /api/v1/models/*` with richer data
        (capabilities, languages, use-case filtering).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicPricingResponse
            Public pricing information

        Examples
        --------
        from talkif import Talkif

        client = Talkif(
            token="YOUR_TOKEN",
        )
        client.billing.get_public_pricing()
        """
        _response = self._raw_client.get_public_pricing(request_options=request_options)
        return _response.data


class AsyncBillingClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawBillingClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawBillingClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawBillingClient
        """
        return self._raw_client

    async def get_balance_summary(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> BalanceSummaryResponse:
        """
        GET /api/v1/billing/balances

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        BalanceSummaryResponse
            Balance summary

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_balance_summary()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_balance_summary(request_options=request_options)
        return _response.data

    async def list_charges(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        charge_type: typing.Optional[ChargeTypeDb] = None,
        status: typing.Optional[ChargeStatus] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        Paginated, filterable charge history for an account.
        Returns charges with entity context (phone number, flow name, contact name)
        from the first associated cost line item.

        Parameters
        ----------
        limit : typing.Optional[int]
            Page size (max 100)

        offset : typing.Optional[int]
            Page offset

        charge_type : typing.Optional[ChargeTypeDb]
            Filter by charge type

        status : typing.Optional[ChargeStatus]
            Filter by status

        start_date : typing.Optional[str]
            Filter from date (inclusive)

        end_date : typing.Optional[str]
            Filter to date (inclusive)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Paginated charge history

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.list_charges()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_charges(
            limit=limit,
            offset=offset,
            charge_type=charge_type,
            status=status,
            start_date=start_date,
            end_date=end_date,
            request_options=request_options,
        )
        return _response.data

    async def get_charge_detail(
        self, charge_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ChargeDetailResponse:
        """
        Detailed charge view with cost line items breakdown.

        Parameters
        ----------
        charge_id : str
            Charge ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ChargeDetailResponse
            Charge detail with line items

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_charge_detail(
                charge_id="chargeId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_charge_detail(charge_id, request_options=request_options)
        return _response.data

    async def get_billing_cost_breakdown(
        self,
        *,
        period: typing.Optional[str] = None,
        start_date: typing.Optional[str] = None,
        end_date: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AnalyticsCostBreakdownResponse:
        """
        Get account-level cost breakdown by category with drill-down to calls.

        ## Query Parameters
        - `period`: `today`, `week`, `month`, or `custom` (default: `week`)
        - `startDate`: Required when `period=custom`
        - `endDate`: Required when `period=custom`

        ## Response
        Returns total costs, breakdown by category (LLM, STT, TTS, telephony),
        breakdown by flow, and recent calls with individual cost breakdowns.

        Parameters
        ----------
        period : typing.Optional[str]
            Time period: today, week, month, or custom

        start_date : typing.Optional[str]
            Start date for custom period

        end_date : typing.Optional[str]
            End date for custom period

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AnalyticsCostBreakdownResponse
            Cost breakdown by category

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_billing_cost_breakdown()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_billing_cost_breakdown(
            period=period, start_date=start_date, end_date=end_date, request_options=request_options
        )
        return _response.data

    async def get_billing_call_cost_breakdown(
        self, call_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CallCostBreakdownResponse:
        """
        Get detailed cost breakdown for a single call with line items.

        Returns all cost line items including usage data (tokens, seconds, characters)
        and rate information for each cost component.

        Parameters
        ----------
        call_id : str
            Call ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CallCostBreakdownResponse
            Call cost breakdown with line items

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_billing_call_cost_breakdown(
                call_id="callId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_billing_call_cost_breakdown(call_id, request_options=request_options)
        return _response.data

    async def list_invoices(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        GET /api/v1/billing/invoices

        Parameters
        ----------
        limit : typing.Optional[int]
            Page size

        offset : typing.Optional[int]
            Page offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Paginated invoice list

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.list_invoices()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_invoices(limit=limit, offset=offset, request_options=request_options)
        return _response.data

    async def get_invoice(
        self, invoice_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> InvoiceResponse:
        """
        GET /api/v1/billing/invoices/{invoiceId}

        Parameters
        ----------
        invoice_id : str
            Invoice ID

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InvoiceResponse
            Invoice details

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_invoice(
                invoice_id="invoiceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_invoice(invoice_id, request_options=request_options)
        return _response.data

    async def get_transaction_history(
        self,
        *,
        limit: typing.Optional[int] = None,
        offset: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedResponse:
        """
        GET /api/v1/billing/transactions

        Parameters
        ----------
        limit : typing.Optional[int]
            Max items to return

        offset : typing.Optional[int]
            Items to skip

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedResponse
            Transaction history

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_transaction_history()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_transaction_history(
            limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def get_public_pricing(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PublicPricingResponse:
        """
        Public endpoint — no authentication required.
        Returns telephony rates, phone number pricing, and recording storage pricing.

        LLM/STT/TTS pricing is served by `GET /api/v1/models/*` with richer data
        (capabilities, languages, use-case filtering).

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PublicPricingResponse
            Public pricing information

        Examples
        --------
        import asyncio

        from talkif import AsyncTalkif

        client = AsyncTalkif(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.billing.get_public_pricing()


        asyncio.run(main())
        """
        _response = await self._raw_client.get_public_pricing(request_options=request_options)
        return _response.data
