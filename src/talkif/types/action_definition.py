# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class ActionDefinition(UniversalBaseModel):
    """
    Action definition for pre/post agent actions

    Actions execute before or after an agent speaks. Unlike functions
    (which are LLM-invoked tools), actions are deterministic steps
    triggered automatically by the flow engine.

    Action types:
    - `"tts_say"`: Speak specific text (config: `{ text: "..." }`)
    - `"play_audio"`: Play an audio file (config: `{ url: "..." }`)
    - `"wait"`: Pause for duration (config: `{ durationMs: 1000 }`)
    - `"webhook"`: Fire a webhook (config: `{ url: "...", method: "POST" }`)
    """

    config: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Action-specific configuration (shape depends on action type)
    """

    type: str = pydantic.Field()
    """
    Action type identifier
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
