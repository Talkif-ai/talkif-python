# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class LlmCapabilities(UniversalBaseModel):
    """
    LLM model capabilities
    """

    function_calling: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="functionCalling"),
        pydantic.Field(alias="functionCalling", description="Supports function/tool calling"),
    ]
    """
    Supports function/tool calling
    """

    has_priority_tier: typing_extensions.Annotated[
        bool,
        FieldMetadata(alias="hasPriorityTier"),
        pydantic.Field(alias="hasPriorityTier", description="Has priority tier option (typically 2x standard pricing)"),
    ]
    """
    Has priority tier option (typically 2x standard pricing)
    """

    json_mode: typing_extensions.Annotated[
        bool, FieldMetadata(alias="jsonMode"), pydantic.Field(alias="jsonMode", description="Supports JSON mode")
    ]
    """
    Supports JSON mode
    """

    reasoning: bool = pydantic.Field()
    """
    Supports a reasoning/thinking mode
    """

    reasoning_effort: typing_extensions.Annotated[
        typing.List[str],
        FieldMetadata(alias="reasoningEffort"),
        pydantic.Field(
            alias="reasoningEffort",
            description="Effort values this model accepts for `reasoningEffort` on a flow's LLM\nservice. Empty means the model takes no effort value — either it has no\nreasoning mode, or it uses an older budget-token API that rejects\n`effort` outright. Clients must offer only these values; anything else is\nstripped server-side before the flow reaches the bot.",
        ),
    ]
    """
    Effort values this model accepts for `reasoningEffort` on a flow's LLM
    service. Empty means the model takes no effort value — either it has no
    reasoning mode, or it uses an older budget-token API that rejects
    `effort` outright. Clients must offer only these values; anything else is
    stripped server-side before the flow reaches the bot.
    """

    streaming: bool = pydantic.Field()
    """
    Supports streaming
    """

    temperature: bool = pydantic.Field()
    """
    Supports custom temperature values (false for gpt-5 family with reasoning on by default)
    """

    vision: bool = pydantic.Field()
    """
    Supports vision/images
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
