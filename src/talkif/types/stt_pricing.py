# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class SttPricing(UniversalBaseModel):
    """
    STT pricing from database
    """

    rate_per_minute_display: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="ratePerMinuteDisplay"),
        pydantic.Field(alias="ratePerMinuteDisplay", description='Human-readable rate (e.g., "$0.0077")'),
    ]
    """
    Human-readable rate (e.g., "$0.0077")
    """

    rate_per_minute_microcents: typing_extensions.Annotated[
        int,
        FieldMetadata(alias="ratePerMinuteMicrocents"),
        pydantic.Field(alias="ratePerMinuteMicrocents", description="Rate per minute in microcents"),
    ]
    """
    Rate per minute in microcents
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
