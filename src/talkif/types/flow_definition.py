# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata
from .agent_definition import AgentDefinition
from .service_definition import ServiceDefinition


class FlowDefinition(UniversalBaseModel):
    """
    The canonical flow definition — single source of truth for flow logic,
    shared by the API and the visual builder.

    Visual nodes (LLM, TTS, STT, prompt, end_call) collapse into properties:
    - Service nodes -> `services.*`
    - Prompt nodes -> `agent.prompt`
    - Action nodes -> `agent.functions[]`
    - Edges are implicit — relationships expressed as data, not connections
    """

    initial_node: typing_extensions.Annotated[
        str,
        FieldMetadata(alias="initialNode"),
        pydantic.Field(
            alias="initialNode", description="Node ID where execution begins (must reference a main_agent node)"
        ),
    ]
    """
    Node ID where execution begins (must reference a main_agent node)
    """

    nodes: typing.Dict[str, AgentDefinition] = pydantic.Field()
    """
    Agent nodes keyed by node ID (e.g., "main_agent_1", "billing_agent")
    """

    services: ServiceDefinition = pydantic.Field()
    """
    Service provider configuration (LLM, TTS, STT)
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
