# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class UserIdleSettings(UniversalBaseModel):
    """
    User-idle hangup settings for a flow.

    Sending this block fully replaces the flow's idle config. Omitting it on update leaves the existing config untouched; sending it with only `enabled` resets the other fields to their defaults. A flow that was never configured has idle hangup ON.
    """

    enabled: bool = pydantic.Field()
    """
    Whether idle hangup is enabled. `false` explicitly disables it.
    """

    end_message: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="endMessage"),
        pydantic.Field(
            alias="endMessage",
            description="Spoken message before hanging up after the last idle prompt (max 1000 chars).",
        ),
    ] = None
    """
    Spoken message before hanging up after the last idle prompt (max 1000 chars).
    """

    max_prompts: typing_extensions.Annotated[
        typing.Optional[int],
        FieldMetadata(alias="maxPrompts"),
        pydantic.Field(alias="maxPrompts", description="Max idle re-prompts before hanging up (1–10)."),
    ] = None
    """
    Max idle re-prompts before hanging up (1–10).
    """

    prompts: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    LLM instructions used to compose each idle re-prompt (max 10).
    """

    reset_on_user_speech: typing_extensions.Annotated[
        typing.Optional[bool],
        FieldMetadata(alias="resetOnUserSpeech"),
        pydantic.Field(alias="resetOnUserSpeech", description="Whether the idle timer resets when the user speaks."),
    ] = None
    """
    Whether the idle timer resets when the user speaks.
    """

    timeout_secs: typing_extensions.Annotated[
        typing.Optional[float],
        FieldMetadata(alias="timeoutSecs"),
        pydantic.Field(
            alias="timeoutSecs", description="Seconds of caller silence before the idle handler fires (1–600)."
        ),
    ] = None
    """
    Seconds of caller silence before the idle handler fires (1–600).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
