# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class RequestSchema(UniversalBaseModel):
    """
    Structured HTTP request shape.

    `pathParams`, `queryParams`, and `body` are each JSON Schema objects.
    Parameter names MUST be unique across the three — the name decides which
    HTTP location a value is sent in.
    """

    body: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    JSON Schema for the request body. Sent as JSON for non-GET methods. Empty object for GET.
    """

    method: str = pydantic.Field()
    """
    HTTP method: GET, POST, PUT, PATCH, DELETE
    """

    path_params: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="pathParams"),
        pydantic.Field(
            alias="pathParams",
            description="JSON Schema for URL path parameters. Each key must appear as `{key}` in `url`.",
        ),
    ] = None
    """
    JSON Schema for URL path parameters. Each key must appear as `{key}` in `url`.
    """

    query_params: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="queryParams"),
        pydantic.Field(
            alias="queryParams",
            description="JSON Schema for query string parameters. Encoded as `?key=value&...` at dispatch time.",
        ),
    ] = None
    """
    JSON Schema for query string parameters. Encoded as `?key=value&...` at dispatch time.
    """

    url: str = pydantic.Field()
    """
    URL. May contain `{paramName}` placeholders matching keys in `pathParams.properties`.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
