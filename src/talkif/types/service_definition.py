# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .llm_service_definition import LlmServiceDefinition
from .stt_service_definition import SttServiceDefinition
from .tts_service_definition import TtsServiceDefinition


class ServiceDefinition(UniversalBaseModel):
    """
    Service provider configuration for LLM, TTS, and STT.

    All fields are optional during draft saves — a flow being built may not
    have all services configured yet. All three are required at publish time
    (enforced by `FlowDefinitionValidator`).
    """

    llm: typing.Optional[LlmServiceDefinition] = None
    stt: typing.Optional[SttServiceDefinition] = None
    tts: typing.Optional[TtsServiceDefinition] = None

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
