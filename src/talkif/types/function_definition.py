# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from ..core.serialization import FieldMetadata


class FunctionDefinition(UniversalBaseModel):
    """
    Tool function definition. Describes the **interface** of a function an agent can call — what it does, not where it executes.

    Function types (unknown types are rejected):
    - `"builtin"`: platform-provided functions. The only valid name is `end_call`. Builtins ignore LLM-supplied arguments, so `properties`/`required` are typically empty.
    - `"http_request"`: user-defined HTTP webhook functions. Requires `config.functionId` referencing a flow function; headers and secrets are stored server-side and never returned by the API.
    """

    config: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    Design-time execution config (NOT LLM params).
    - builtin end_call: null
    - http_request: `{ "functionId": "UUID" }` (required, references flow_functions)
    """

    description: str = pydantic.Field()
    """
    LLM tool description — tells the model when to invoke this function
    """

    name: str = pydantic.Field()
    """
    Function name — identifier and LLM tool name.
    For builtins: must be `end_call` (the only supported builtin today).
    For http_request: resolved from flow_functions DB record at publish time.
    """

    param_bindings: typing_extensions.Annotated[
        typing.Optional[typing.Dict[str, typing.Any]],
        FieldMetadata(alias="paramBindings"),
        pydantic.Field(
            alias="paramBindings",
            description='Per-parameter source bindings, resolved at publish time from the referenced flow function. Only populated for `http_request` functions.\n\nShape: `{ "paramName": { "source": "llm|call_context|static", ... } }`\n\nRead-only in the published definition; edit bindings on the flow function.',
        ),
    ] = None
    """
    Per-parameter source bindings, resolved at publish time from the referenced flow function. Only populated for `http_request` functions.
    
    Shape: `{ "paramName": { "source": "llm|call_context|static", ... } }`
    
    Read-only in the published definition; edit bindings on the flow function.
    """

    properties: typing.Optional[typing.Dict[str, typing.Any]] = pydantic.Field(default=None)
    """
    JSON Schema for parameters the LLM extracts from conversation.
    For builtins: typically empty (no LLM params needed).
    For http_request: resolved from flow_functions DB record at publish time.
    """

    required: typing.Optional[typing.List[str]] = pydantic.Field(default=None)
    """
    Required parameter names from `properties`
    """

    transition_to: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="transitionTo"),
        pydantic.Field(
            alias="transitionTo", description='Target node/state after invocation (e.g., "good_bye_1753" for end_call)'
        ),
    ] = None
    """
    Target node/state after invocation (e.g., "good_bye_1753" for end_call)
    """

    type: typing.Optional[str] = pydantic.Field(default=None)
    """
    Function type: "builtin" or "http_request"
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow
